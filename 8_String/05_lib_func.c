#include <stdio.h>
#include <string.h>
// We have to include string.h library to use all the inbuild string functions.

int main()
{
    /* gets() function allows us to enter multi line string.
    gets("Muhammad Mushfiqur Rahman");

    puts() function is used to show string output.
    puts("Muhammad Mushfiqur Rahman");

    strlen() function is used to calculate the length of the string.
    strlen(string_name);

    strcpy() function is used to copy the second string into the first string.
    strcpy(first_string, second_string);

    strcat() function is used to concatenate 2 strings.
    strcat(first_string, second_string);

    strcmp() function is used to compare 2 strings in dictionary.
    strcmp(first_string, second-string);
    It will return 1 if the second_string exists before the first_string in dictionary. And it will return -1 if the second_string exists after first_string.
*/
    return 0;
}