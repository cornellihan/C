#include <stdio.h>

int main()
{
    // What data type will 3.0/8 - 2 return ?
    3.0 / 8 - 2;
    // When we write a "Float" data types like the top, it will be defined as a "Double" data type most of the time.
    // It actually depends on the compiler we use. By default the compiler define it as "Double" data type, but we can also edit our compiler.
    return 0;
}