#include <stdio.h>
// Print something on the console 100 times using Decrease Operator.

int main()
{
    int i = 100;
    while (i >= 1)
    {
        printf("Sorry :( \n");
        i--;
    }

    return 0;
}