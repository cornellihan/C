#include <stdio.h>

int main()
/* There are some relational operators-
    ==, !=, >, >=, <, <=  
    */
{
    if (-1)
    {
        printf("Any non zero value is evaluated as true.");
    }
    return 0;
}